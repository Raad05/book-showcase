<h2>Setup Instructions: </h2>

```yaml
# Clone the repository and open the project directory in your desired IDE
git clone https://github.com/Raad05/book-showcase.git

# Run to install the dependencies.
npm install

# Run to start the proxy server
node proxy/proxy-server.js

# Start the local server
npm run dev
```
